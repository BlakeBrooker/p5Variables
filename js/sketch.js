function setup() {
    var canvas = createCanvas(800, 800);
    canvas.parent("p5container");
}
var firstName = 'blake';

var thisIsAnInterger = 100;

var thisIsABoolean = true:

var thisIsAnUndefinedVar;

var thisIsNull= null;

var ellipsex = 400;

var ellipseY = 200;

function setup() {
var canvas = createCanvas (800, 800);
canvas.parent ("p5container");
background (220);
console. log (thisIsAnInterger);
}

function draw() {

fil1(255, 255, 255);
ellipse (mouseX, mouseY, 100);

console. log (mouseX, mouseY)
}
